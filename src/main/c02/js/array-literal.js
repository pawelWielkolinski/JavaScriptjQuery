var colors;
colors = ['white', 'black', 'other'];

var numColors;
numColors - colors.length;

var el = document.getElementById('colors');
el.textContent = colors[0]; //returns text content (replace all child nodes)